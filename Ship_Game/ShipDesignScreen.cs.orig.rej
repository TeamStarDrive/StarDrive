--- Ship_Game/ShipDesignScreen.cs.orig	
+++ Ship_Game/ShipDesignScreen.cs.orig	
@@ -300,6 +300,17 @@
 						button.Active = true;
 					}
 				}
+                else if (str == "broadside_left")
+                {
+                    if (this.CombatState != Ship_Game.Gameplay.CombatState.BroadsideLeft)
+                    {
+                        button.Active = false;
+                    }
+                    else
+                    {
+                        button.Active = true;
+                    }
+                }
 				else if (str != "orbit_right")
 				{
 					if (str == "evade")
